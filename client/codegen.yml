schema: schema.gql
documents:
  - src/graphql/**/*.ts
  - '!src/graphql/generated/**'
generates:
  src/graphql/generated/:
    preset: client
    presetConfig:
      fragmentMasking: false
    config:
      useTypeImports: true
